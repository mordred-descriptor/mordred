package:
  name: mordred
  version: 0

build:
  number: 0
  script:
    - $PYTHON setup.py install

requirements:
  build:
    - python
    - setuptools

    - networkx
    - numpy
    - six
    - pyyaml
    - rdkit
    - enum34 [py27]
    - tqdm

  run:
    - python
    - networkx
    - numpy
    - six
    - pyyaml
    - rdkit
    - enum34 [py27]
    - tqdm

test:
  requires:
    - nose

  commands:
    - cd $SRC_DIR/tests && nosetests
